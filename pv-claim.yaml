apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: login-pv-claim
spec:
  storageClassName: manual
  volumeName: login-pv-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
